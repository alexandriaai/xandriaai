import * as vscode from 'vscode';

export class SnippetPanel {
  public static createOrShow(extensionUri: vscode.Uri) {
    console.log('SnippetPanel created');
    // Webview logic to be added later
  }
}
